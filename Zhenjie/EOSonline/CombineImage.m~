%%
load('../Library');
list={};
var=[];
Nsat=315;
Nmat=
%%
folder='';
AveImgList={};
Varlist=[];
while length(list)>0
    var_sel=var(1);
    list_sel=list(var==var_sel);
    Nt=length(list_sel);
    Nimgtemp=cell(Nt,1);
    for i=1:Nt
        Nimgtemp{i}=fitsread([folder,list_sel{i},'.fits']);
    end
    AveImglist=[AveImglist;AverageImg(Nimgtemp)];
end